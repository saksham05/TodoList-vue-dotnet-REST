using System.IO;
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Design;
using Microsoft.Extensions.Configuration;

namespace Todo.Models
{
   // public class TaskContextFactory : IDesignTimeDbContextFactory<TaskContext>
  //  {
       // public TaskContext CreateDbContext(string[] args)
      //  {
         //   var optionBuilder = new DbContextOptionsBuilder<TaskContext>();
         //   optionBuilder.UseSqlite(args[0]);

         //   return new TaskContext(optionBuilder.Options);
     //   }

  // }
}
